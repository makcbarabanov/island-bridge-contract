# Runbook (черновик)

Короткие заметки для Макса и Форжа: куда смотреть при сбоях, где логи, как перезапускать.

## Bloom / Bridge

- Репозиторий: `bridge`, процесс: `systemctl --user` → `bridge-bot.service`, скрипт `./bridge restart|status|logs`.
- Конфиг: `.env` на сервере с ботом (не в git).
- Логи приложения: `bridge_bot.log` (если пишется в файл).

## Остров

- Прод: по инфраструктуре Форжа (nginx, `island.service`, и т.д.) — дописать фактические команды.

## Контракты

Актуальная спека: этот репозиторий (handbook). Расхождение «бот шлёт X, API ждёт Y» — сначала смотреть `api.md`, потом код `main.py` / `island_api.py`.
